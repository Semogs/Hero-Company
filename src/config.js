const apiBaseUrl = 'https://api.github.com';

const endpoints = {
  registerTrainerEndpoint: apiBaseUrl + '/auth/register',
  loginTrainerEndpoint: apiBaseUrl + '/auth/login'
};

export default endpoints;
